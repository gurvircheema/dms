<% content_for :topbar_heading do %>
  New Equipment
<% end %>
<% content_for :topbar_buttons do %>
  <%= link_to 'Back', admin_equipment_path(@equipment), class: 'btn btn-sm btn-secondary' %>
<% end %>
<%= render 'admin/shared/topbar' %>
<%= render 'form' %>