<% content_for :topbar_heading do %>
  New Violation Ticket
<% end %>
<%= render 'admin/shared/topbar' %>

<%= simple_form_for([:admin, :driver, @violation_ticket], html: { class: 'form' }) do |f| %>
  <%= render 'form', f: f %>
<% end %>