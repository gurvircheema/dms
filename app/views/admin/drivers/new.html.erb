<div class='primary-header'>
  New Driver
  <span class='float-right'>
    <%= link_to 'Back', back_url(@driver), class: 'btn btn-sm btn-secondary' %>
  </span>
</div>
<%= simple_form_for([:admin, @driver], html: { class: 'form'}) do |f| %>
  <%= render 'form', f: f %>
<% end %>
